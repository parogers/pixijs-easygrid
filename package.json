{
    "name": "@parogers/pixijs-grid",
    "version": "0.0.1",
    "description": "A \"good enough\" grid sprite for PixiJS",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc",
        "serve": "esbuild src/index.ts --bundle --outfile=examples/pixijs-grid.js --global-name=easygrid --external:pixi.js --watch --serve --servedir=.",
        "build-web": "esbuild src/index.ts --bundle --outfile=examples/pixijs-grid.js --global-name=easygrid --sourcemap --external:pixi.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [],
    "author": "",
    "license": "GPL-3.0",
    "peerDependencies": {
        "pixi.js": ">=8.0.2"
    },
    "devDependencies": {
        "esbuild": "^0.24.0",
        "typescript": "^5.6.3"
    }
}
